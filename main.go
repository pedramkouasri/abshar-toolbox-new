package main

func main() {
	// wg := new(sync.WaitGroup)
	// loading := loading.NewLoading([]string{"A", "B", "C"}, wg)
	// loading.Update("A", 30)
	// loading.Update("A", 40)
	// loading.Update("A", 50)
	// loading.Update("B", 20)
	// loading.Update("C", 10)
	// loading.Update("C", 23)
	// loading.Update("A", 65)
	// loading.Update("C", 40)
	// loading.Update("B", 100)
	// // loading.Close()

	// defer wg.Wait()
}
